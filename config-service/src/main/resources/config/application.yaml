eureka:
  client:
    serviceUrl:
      defaultZone: http://${EUREKA_ADDRESS:localhost:8761}/eureka

auth:
  service:
    app:
      name: "auth-service"
    context:
      path: "/api/auth"

user:
  service:
    app:
      name: "user-service"
    context:
      path: "/api/user"